package com.kodilla.testing;

import com.kodilla.testing.user.SimpleUser;
import com.kodilla.testing.calculator.Calculator;

public class TestingMain {
    public static void main(String[] args) {

        /*
        SimpleUser simpleUser = new SimpleUser("theForumUser");

        String result = simpleUser.getUsername();

        if (result.equals("theForumUser")) {
            System.out.println("test OK");
        } else {
            System.out.println("Error!");
        }

        // First Unit test

        System.out.println("Beginning first unit test fo Calculator class:");
        Calculator calculator = new Calculator();

        int testAValue = 14;
        int testBValue = 2;

        int addResult = calculator.add(testAValue, testBValue);

        if (addResult == (testAValue + testBValue)) {
            System.out.println("Result of addiction values " + testAValue + " and " + testBValue + " - OK");
        } else {
            System.out.println("Error in calculated addition");
        }

        int subtractResult = calculator.subtract(testAValue, testBValue);

        if (subtractResult == (testAValue - testBValue)) {
            System.out.println("Result of subtraction values " + testAValue + " and " + testBValue + " - OK");
        } else {
            System.out.println("Error in calculated subtraction");
        }
        */
    }

}
